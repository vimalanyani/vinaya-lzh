---
import { basePath } from "../utils";

interface Props extends astroHTML.JSX.AnchorHTMLAttributes {
  href: string;
  text: string;
}

const { href, text, class: styles } = Astro.props;
---

<a
  class:list={["underline hover:text-amber-700", styles]}
  href={`${basePath}${href}`}>{text}</a
>
